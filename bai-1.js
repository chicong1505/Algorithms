/* 
    Đề bài: Viết hàm tìm phần tử lớn thứ hai trong một mảng các số nguyên.
    Ví dụ:
        Input: [10, 5, 20, 8]
        Output: 10
*/

const arr = [10, 5, 20, 8];

function findSecondLargest(arr) {
  if (arr.length < 2) return null;
  let largest = -Infinity,
    secondLargest = -Infinity;
}
